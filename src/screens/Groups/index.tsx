import { StyleSheet, Text, View } from 'react-native';
import * as S from './styles';
// import { Container, Title } from './styles'; // The same import but with different types.


export default function Groups() {
  return (
    <S.Container>
      <S.Title>Groups</S.Title>
    </S.Container>
  );
}
